[build]
  command = "bundle install && bundle exec jekyll build && ./scripts/check-public-showcase.sh"
  publish = "_site"

[build.environment]
  RUBY_VERSION = "3.3.6"
  JEKYLL_ENV = "production"

[[redirects]]
  from = "/*"
  to = "/404.html"
  status = 404
